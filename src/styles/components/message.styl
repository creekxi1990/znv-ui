.znv-message {
  font-size: $base-font-size;
  position: fixed;
  z-index: 1010;
  width: 100%;
  top: 30px;
  left: 0;
  pointer-events: none;
}
.znv-message-notice {
  box-sizing: border-box;
  padding: 8px;
  text-align: center;
  transition: height .3s ease-in-out, padding .3s ease-in-out;
}

.znv-message-notice-content {
  display: inline-block;
  pointer-events: all;
  padding: 10px 16px;
  border-radius: $border-base-radius;
  box-shadow: 0 1px 6px 1px rgba(0, 0, 0, .15);
  background: #fff;
  position: relative;
  &-text {
    display: flex;
    align-items: center;
  }
  i {
    font-size: 18px;
    vertical-align: baseline;
    margin-right: 8px;
  }
}

.znv-message-notice-close {
  position: absolute;
  right: 0;
  top: 11px;
  color: $color-text-default;
  outline: 0;
  cursor: pointer;
  i {
    font-size: 22px;
    color: $color-info;
    transition: color .2s ease;
    position: relative;
    top: -2px;
  }
}


.znv-message-notice-closable {
  .znv-message-notice-content {
    position: relative;
    padding-right: 32px;
  }
}
.znv-message-notice-primary i {
  color: $color-primary;
}
.znv-message-notice-success i {
  color: $color-success;
}
.znv-message-notice-warning i {
  color: $color-warning;
}
.znv-message-notice-danger i {
  color: $color-danger;
}
// move-up
.move-up-enter-active,
.move-up-leave-active {
  transition: all .3s cubic-bezier(.55, 0, .1, 1);
}
.move-up-enter,
.move-up-leave-to {
  transform-origin: 0 0;
  transform: translateY(-100%);
  opacity: 0;
}
